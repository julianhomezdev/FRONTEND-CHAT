export default function ChatCard(userPhoto, userName, lastMessage) {

    // This is the component for the chat cards with the main info of the user in the
    // sidebar chats
    return(

        <></>

    )
}